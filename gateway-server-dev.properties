# Optional override when using Spring Cloud Config with profile `dev` for gateway-server.
# Keeps the transactions route from matching `/api/payout/**` (must align with ApiGateway application.properties).
spring.cloud.gateway.server.webflux.routes[8].predicates[0]=Path=/api/transactions/**,/api/analytics/**,/api/status/webflux/**
